# Range через .. — даёт массив [start, end-1]
для i в 1..5
    вывод(i)
конец

# Можно сохранить в переменную
конст диапазон = 0..3
вывод(диапазон)
вывод(длина(диапазон))

# С отрицательными границами
для i в -2..3
    вывод(i)
конец

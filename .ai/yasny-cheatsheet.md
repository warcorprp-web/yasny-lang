# Шпаргалка по Ясному

Краткий справочник по синтаксису для быстрого использования. Полное
описание — в README.md.

## Базовые правила

- Расширение файла: `.ya`.
- Комментарий: только `#`. Многострочного комментария нет.
- Точка с запятой не нужна.
- Каждый блок открывается ключевым словом и закрывается `конец`.
- Можно писать тело блока в одну строку через двоеточие:
  `если x > 0: вывод(x)`.
- Строки в двойных кавычках поддерживают интерполяцию: `"{x + 1}"`.

## Переменные

```
конст имя = "Аня"          # неизменяемая (основная форма)
перем счётчик = 0          # изменяемая

счётчик = счётчик + 1
счётчик += 5
```

## Типы данных

```
конст число = 42
конст дробь = 3.14
конст текст = "строка"
конст флаг = да           # или нет
конст пустота = пусто
конст массив = [1, 2, 3]
конст словарь = {"имя": "Иван", "возраст": 30}
```

## Условия

```
если условие
    ...
иначеесли другое
    ...
иначе
    ...
конец
```

`если` — это выражение, может возвращать значение:

```
конст x = если y > 0: y иначе: -y
```

## Циклы

```
для i от 1 до 10           # включительно
    ...
конец

для i от 0 до 100 по 5     # с шагом
    ...
конец

для элемент в коллекция
    ...
конец

пока условие
    ...
конец

прервать       # выйти из цикла
продолжить     # к следующей итерации
```

## Функции

```
функция плюс(a, b)
    вернуть a + b
конец

функция степень(основание, показатель = 2): основание * основание

функция сумма_всех(...числа)
    перем итог = 0
    для n в числа: итог += n
    вернуть итог
конец

конст удвоить = x => x * 2
```

## Классы

```
класс Точка
    функция создать(x, y)
        это.x = x
        это.y = y
    конец

    функция длина()
        вернуть корень(это.x * это.x + это.y * это.y)
    конец
конец

конст p = Точка(3, 4)
вывод(p.длина())
```

Наследование:

```
класс Кот наследует Животное
    функция создать(имя, цвет)
        родитель.создать(имя)
        это.цвет = цвет
    конец
конец
```

`создать` — имя конструктора. `это` — текущий экземпляр.
`родитель` — родительский класс.

## Ошибки

```
попытка
    рискованное()
поймать ошибка
    вывод("Сбой: {ошибка}")
всегда
    очистка()
конец

бросить "что-то сломалось"
```

## Сопоставление с образцом

```
совпадение значение
    когда 1: "один"
    когда 2: "два"
    иначе: "другое"
конец
```

## Тесты

```
тест "название"
    проверить условие
конец
```

## Современные конструкции

```
# Цепочка вычислений (pipe)
числа |> фильтр(x => x > 0) |> сумма

# Optional chaining
пользователь?.адрес?.город ?? "не указан"

# Spread
конст оба = [...первый, ...второй]

# Деструктуризация
конст [a, b, ...остальное] = массив

# List comprehension
конст квадраты = [x * x для x в [1, 2, 3, 4] если x % 2 == 0]

# Срезы
числа[1:4]    # с 1 по 3 включительно
числа[:3]     # первые три
числа[-2:]    # последние два
```

## Стандартная библиотека

### Часто используемое

```
вывод(значение)
ввод(приглашение)
длина(массив_или_строка)
тип(значение)
строка(x)         # преобразовать в строку
число(s)          # преобразовать в число
```

### Массивы

```
добавить(массив, элемент)
удалить(массив, индекс)
фильтр(массив, x => условие)
преобразовать(массив, x => выражение)
свернуть(массив, (накоп, x) => выражение, начальное)
сортировать(массив)
сумма(массив)
найти(массив, x => условие)
все(массив, x => условие)
любой(массив, x => условие)
уникальные(массив)
```

### Строки

```
верхний(s), нижний(s), обрезать(s)
разделить(s, разделитель)
соединить(массив, разделитель)
заменить(s, старое, новое)
подстрока(s, начало, конец)
содержит(s, что), начинается_с(s, префикс), заканчивается_на(s, суффикс)
```

### Словари

```
ключи(словарь)
значения(словарь)
получить(словарь, ключ, по_умолчанию)
```

### Числа

```
мин(a, b), макс(a, b)
абс(x), округл(x)
корень(x), степень(основание, показатель)
```

### Стандартные модули

Доступны без импорта:

```
мат.пи, мат.е, мат.бесконечность
мат.sin(x), мат.cos(x), мат.tan(x)
мат.лог(x), мат.лог2(x), мат.лог10(x)
мат.пол(x), мат.потолок(x)

время.сейчас()                # Unix timestamp
время.метка()                 # миллисекунды
время.строка([метка], [формат])
время.год(), время.день(), время.час()
время.спать(миллисекунды)

json.создать(объект)          # объект -> JSON-строка
json.разобрать(строка)        # JSON-строка -> объект

регвыр.найти(текст, шаблон)
регвыр.заменить(текст, шаблон, замена)
регвыр.совпадает(текст, шаблон)

http.получить(url)
http.пост(url, тело)
http.сервер(порт, обработчик)

файлы.читать(путь)
файлы.записать(путь, содержимое)
файлы.существует(путь)
случайное.число()             # 0..1
случайное.целое(мин, макс)
```

## Модули

Файл `модуль.ya`:

```
экспорт функция приветствие(имя)
    вернуть "Привет, {имя}!"
конец
```

Использование:

```
импорт м из "модуль.ya"
вывод(м.приветствие("Аня"))
```

## Чего в языке нет

- Аннотаций типов (язык динамический).
- `++`, `--` (используйте `+= 1`, `-= 1`).
- Нескольких видов комментариев. Только `#`.
- Старых форм: `пусть`, `var`, `процедура`, `возврат`, `истина`,
  `ложь`, `//`. Они удалены — используйте канонические.
